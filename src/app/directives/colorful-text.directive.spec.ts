import { ColorfulTextDirective } from './colorful-text.directive';

describe('ColorfulTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorfulTextDirective();
    expect(directive).toBeTruthy();
  });
});
